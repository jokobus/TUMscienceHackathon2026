# WEave Employee App — environment

# Leave EMPTY to run fully offline against the bundled mock data (demo mode).
# Set to your backend origin (no trailing slash) to switch the whole app to the
# real REST + WebSocket backend (WEAVE_MASTER §6/§7), e.g.:
#   EXPO_PUBLIC_API_BASE_URL=https://api.weave.de
# Locally:
#   EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
EXPO_PUBLIC_API_BASE_URL=
