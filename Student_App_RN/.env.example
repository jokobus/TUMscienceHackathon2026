# WEave Student App — environment

# Backend origin (no trailing slash). The app uses the real REST + WebSocket
# backend (WEAVE_MASTER §6/§7).
#   Local / iOS simulator : http://localhost:8000
#   Android emulator      : http://10.0.2.2:8000
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
