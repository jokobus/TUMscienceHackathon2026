# ── WEave deployment config — copy to deploy/.env and fill in ─────────────────

# Public domains (point both A-records at your Hetzner server's IP).
DOMAIN_APP=app.weave.example.com
DOMAIN_API=api.weave.example.com

# Database (change the password for production!).
POSTGRES_USER=weave
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=weave

# Backend secrets.
JWT_SECRET=generate-a-long-random-string
SEED_ON_STARTUP=true            # seeds demo data on first boot; set false after

# AI keys (optional).
ANTHROPIC_API_KEY=              # backend AI features (Backend/app/ai.py)
OPENROUTER_API_KEY=             # dashboard /api/assistant route

# Dashboard data source:
#   true  → offline demo data (safest for the pitch; backend not required)
#   false → live, calls https://${DOMAIN_API}
NEXT_PUBLIC_USE_MOCKS=true
