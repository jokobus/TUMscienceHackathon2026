# When a real backend exists, point the API client at it and the mock layer
# will fall back to live REST + WebSocket. Leave unset to use bundled mock data.
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_WS_URL=
